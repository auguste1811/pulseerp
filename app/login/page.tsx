import { loginAction } from "./actions";
export default async function Login({searchParams}:{searchParams:Promise<{error?:string}>}){const p=await searchParams;return <main className="login-wrap"><section className="login-card"><div className="brand"><span className="brand-logo">P</span><span style={{color:"#111827"}}>PulseERP</span></div><h1>Connexion</h1><p className="muted">Accédez à votre espace entreprise.</p>{p.error&&<p className="alert">Email ou mot de passe incorrect.</p>}<form action={loginAction} className="form"><input name="email" type="email" defaultValue="demo@pulseerp.fr" required/><input name="password" type="password" defaultValue="Pulse123!" required/><button className="btn primary">Se connecter</button></form><p className="muted" style={{fontSize:13}}>Compte de démonstration prérempli.</p></section></main>}